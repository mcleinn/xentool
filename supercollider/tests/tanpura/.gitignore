# Output of tanpura_test_rig.scd + analyze_renders.py.
# Recreated on every test run — committing it would balloon the repo
# with binary WAVs and per-render artefacts that anyone can regenerate
# from the test scripts.
renders/
replays/
bisect_*/
v*_renders/
v*_replays/
feature_diff.csv
